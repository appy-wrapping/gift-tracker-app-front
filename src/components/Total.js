const Total = ({ gifts }) => (


    <>
        <h2 className="footer">{friendName}</h2>
    </>
);

export default Total;

// bring in all gifts
// use reducer to get the total of all the prices
// return that